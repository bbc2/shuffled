language: python
cache: pip
python:
  - "2.7"
  - "3.4"
  - "3.5"
jobs:
  include:
    - python: "3.8"
      script:
        - pip install black
        - make check
install: pip install --editable .[dev]
script:
  - make check-tests
